{{- if .Values.mysql.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "jira-helm.fullname" . }}-mysql-config
  labels:
    {{- include "jira-helm.labels" . | nindent 4 }}
data:
  my.cnf: |
    {{- .Values.mysql.configuration | nindent 4 }}
{{- end }}