apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: wordpress
  name: phpmyadmin-ingress
  annotations:
    cert-manager.io/cluster-issuer: "lets-encrypt"
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  tls:
    - hosts:
      - erfanmazraei.3ttest.ir
      secretName: echo-tls-phpmyadmin
  rules:
  - host: erfanmazraei.3ttest.ir
    http:
      paths:
      - path: /dbadmin(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: phpmyadmin-service
            port:
              number: 80
